# weather server configuration 
flightList.url = http://localhost:8081/api/flights
 
# database 
# hibernate 
spring.datasource.url = jdbc:mysql://localhost:3306/flightreservation?serverTimezone=UTC&&useLegacyDatetimeCode=false
spring.datasource.username = root
spring.datasource.password = test001

#if table doesn't exists, create it.
spring.jpa.hibernate.ddl-auto=update
